/**
 * if (user)
 *      => if(contract) // contract is in due 
 *          pay penalty fee, not receive deposit
 *      => else
 *          reveive deposit
 * if (owner)
 *      => if(contract) // contract is in due 
 *          pay penalty fee, pay deposit
 *      => else
 *          pay deposit
 */