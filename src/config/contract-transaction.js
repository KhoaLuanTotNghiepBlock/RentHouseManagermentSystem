/**
 * if (user)
 *      => if(contract) // contract is in due 
 *             if(ownerArgree)
 *                  receive deposit
 *              else
 *                  pay penalty fee, not receive deposit
 *      => else
 *          reveive deposit
 * if (owner)
 *      => if(contract) // contract is in due 
 *              if(userAgree)
 *                  back deposit
 *              else
 *                  pay penalty fee, back deposit
 *      => else
 *          back deposit
 */